package com.example.educationapplication.search.dataObjects.Session;

public class UserLoginSession extends State{

    public UserLoginSession(UserSession session) { super(session);
    }

    @Override
    public void handle(Event event) {
    }
}
